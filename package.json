{
  "name": "threejs-dispose-vram-audit",
  "version": "1.0.0",
  "description": "Detecting and preventing GPU memory leaks in Three.js single-page applications: recursive scene tree disposal, reference-counted texture caching, and renderer.info VRAM tracking.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "@types/three": "0.185.1",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
